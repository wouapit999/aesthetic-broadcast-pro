[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime for Netlify (Route Handlers run as Netlify Functions).
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Drain the WhatsApp send queue every minute via a Netlify Scheduled Function.
# (Defined in netlify/functions/dispatch.mjs with its own `schedule` config.)
